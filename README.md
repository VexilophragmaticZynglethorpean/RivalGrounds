# RivalGrounds
git clone https://github.com/VexilophragmaticZynglethorpean/RivalGrounds --recurse-submodules
\\
Then run build.bat (on Windows) or build.sh (for Linux)
Binary file will be in bin directory.
